@model Web.Core.Routers.ViewModels.RouterViewModel

<form asp-controller="Routers" asp-action="AddDetail" id="frmRouterDetail" method="post">
    <div class="modal-body">
        <div class="container">
            <div class="row">
                <input asp-for="Id" />
                <div class="form-group col-md-12">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="form-group col-md-12">
                    <label asp-for="AccessNo" class="control-label"></label>
                    <input asp-for="AccessNo" class="form-control" />
                    <span asp-validation-for="AccessNo" class="text-danger"></span>
                </div>

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">خروج</button>
                @{ var url = Model.Id > 0 ? Url.Action("EditDetail","Routers") : Url.Action("AddDetail", "Routers");}
                <button onclick="event.preventDefault();saveAndupdate($(this.form), '@url');" type="button" class="btn btn-primary">ذخیره</button>
            </div>
        </div>
    </div>
</form>
